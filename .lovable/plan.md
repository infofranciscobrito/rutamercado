Tarea: cambiar el estilo del botón "Registro de productores" en `src/routes/productores.tsx`.

Cambios exactos:
- Fondo: `#54b678` sólido (verde primario)
- Texto: blanco (`text-white`)
- Borde: eliminar (`border-none` o quitar clases de borde)
- Hover: fondo `#439660` (verde más oscuro), texto blanco, transición `200ms ease-out`
- No afectar ningún otro botón del sitio

Archivo a editar: `src/routes/productores.tsx` — ajustar las clases del `<button>` del registro de productores.

No requiere nuevos archivos ni dependencias.